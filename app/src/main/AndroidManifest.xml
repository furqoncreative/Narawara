<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sera5.narawara">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:name=".MyApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.bugsnag.android.API_KEY" android:value="3d58f6c109036d3269bee7b9de29a20f"/>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Custom" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Custom">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Custom" />
        <activity android:name=".QuizActivity" >

        </activity>
        <activity android:name=".HasilQuiz"
            android:theme="@style/Custom"></activity>
        <activity android:name=".PahlawanDetailsActivity" />
        <activity android:name=".ViewArtikelActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".DinoActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".ResetPasswordActivity" />
    </application>

</manifest>